export default class Tarefas{
    constructor( nomeTarefa, dataInicio, dataTermino, status, foto)
    {
        this.nomeTarefa = nomeTarefa;
        this.dataInicio = dataInicio;
        this.dataTermino = dataTermino;
        this.status = status;
        this.foto = foto;
        
    }
}
